---
const { label, text, type, id, placeholder } = Astro.props;
---

<div class="flex flex-col gap-1">
	<label for={label}>{text}:</label>
	<input
		type={type}
		placeholder={placeholder}
		id={id}
		class="outline-none border-[1px] border-gray-300 transition-all ease-in-out duration-200 py-[0.3rem] px-[0.5rem] rounded-sm focus:border-blue-400 focus:shadow-sm"
	/>
</div>
