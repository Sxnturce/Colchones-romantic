---
const { ico, title, info } = Astro.props;
---

<div class="flex flex-col items-center justify-center gap-3 w-full p-4">
	<div class="rounded-full bg-slate-300 opacity-50 py-[1.25rem] px-[1.32rem]">
		<i class={`${ico} text-5xl text-gray-800`} ></i>
	</div>
	<h2 class="text-center font-bold">{title}</h2>
	<p class="text-center text-font">{info}</p>
</div>
